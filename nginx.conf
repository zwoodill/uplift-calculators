server {
    listen 80;
    server_name systems.uplift.us;

    root /usr/share/nginx/html;
    index water-systems-planner.html;

    # The repo is bind-mounted whole; never serve .git or other dotfiles.
    location ~ /\. {
        deny all;
    }
}
